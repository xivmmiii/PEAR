import type { Metadata } from "next";
export const metadata: Metadata = { title: "Join PEAR — PEAR", description: "Create your PEAR shopper or seller account." };
export default function SignUpLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
