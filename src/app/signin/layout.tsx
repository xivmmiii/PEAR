import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sign in — PEAR", description: "Sign in to your PEAR shopper or seller account." };
export default function SignInLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
